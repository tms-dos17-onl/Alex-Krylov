name: My_pipeline
on:
  push:
    branches:
      - main
jobs:
  my_test:
    runs-on: ubuntu-latest
    steps:
      - name: Print
        run: |
          git clone https://github.com/tms-dos17-onl/spring-petclinic.git
           
     
     
