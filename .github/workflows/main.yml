name: My_pipeline
on:
  push:
    branches:
      - main
jobs:
  my_test:
    runs-on: ubuntu-latest
    steps:
      - name: Print
        run: |
          git --version
          git config user.name 'MyUsername'
           
     
     
